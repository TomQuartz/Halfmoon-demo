apiVersion: v1
kind: Service
metadata:
  name: dynamodb-service
spec:
  selector:
    app.kubernetes.io/name: dynamodb-local
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  clusterIP: 10.96.128.129
