apiVersion: v1
kind: Pod
metadata:
  name: dynamodb-local
  labels:
    app.kubernetes.io/name: dynamodb-local
spec:
  # nodeName: gateway1
  containers:
  - name: dynamodb-local
    image: amazon/dynamodb-local:latest
    ports:
      - containerPort: 8000
        hostPort: 8000
  restartPolicy: Always
